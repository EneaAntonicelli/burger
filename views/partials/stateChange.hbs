
	{{!-- {{burgerName}} --}}

	<button class="btn btn-success purchase" data-id="{{id}}" data-newPurchase="{{purchased}}">
		{{#if purchased}}REMOVE{{else}}PURCHASE{{/if}}
	</button>

	<button class="btn btn-warning deleteBurger" data-id="{{this.id}}">DELETE</button>
